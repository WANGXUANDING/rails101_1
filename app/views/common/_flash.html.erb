<% if flash.any? %>
  <% user_facint_flashes.each do |key, value| %>
    <div class="alert alert-dismissable laert-<%= flash_class(key) %>">
      <button class="close" data-dismiss="alert">×</button>
      <%= value %>
    </div>
  <% end %>
<% end %>
